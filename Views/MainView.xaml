<Window x:Class="Todo_List_WPF.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Todo_List_WPF.ViewModels"
        xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Icon="pack://application:,,,/Resources/list.ico"
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        Title="TODO List" Height="450" Width="800" MinWidth="600" MinHeight="350">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DatePicker SelectedDate="{Binding SelectedDate}" Margin="0,0,260,0" Grid.Row="0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0">
            <Button Content="Add" Command="{Binding AddTaskCommand}" Width="75" Margin="5" />
            <Button Content="Edit" Command="{Binding EditTaskCommand}" Width="75" Margin="5" />
            <Button Content="Remove" Command="{Binding RemoveTaskCommand}" Width="75" Margin="5" />
        </StackPanel>

        <ListBox ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedItem}" Grid.Row="1" Margin="5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <CheckBox 
                            IsChecked="{Binding IsCompleted, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}" 
                            VerticalAlignment="Center" 
                            Margin="5"
                            Checked="OnTaskCompletionChanged"
                            Unchecked="OnTaskCompletionChanged"/>
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" Margin="5" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <tray:NotifyIcon Icon="pack://application:,,,/Resources/list.ico"
                         TooltipText="TODO List"
                         LeftClick="OnNotifyIconLeftClick">
            <tray:NotifyIcon.Menu>
                <ContextMenu>
                    <MenuItem Header="Show" Click="ShowAppFromTray" />
                    <Separator />
                    <MenuItem Header="Exit" Click="ExitApp" />
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>



    </Grid>
</Window>
